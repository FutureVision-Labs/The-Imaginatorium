# Google Gemini API key for live agency agents (optional — mock mode works without it)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: override default model (gemini-2.0-flash)
# GEMINI_MODEL=gemini-2.0-flash

# Agency prototype server port (default 3847)
# AGENCY_PORT=3847
